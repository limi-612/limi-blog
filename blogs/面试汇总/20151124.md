---
title: 面试汇总
date: 2025/11/24
tags:
 - React
 - Webpack
 - 网络请求
 - 数据处理
categories:
 - 面试汇总
---

<ReadAloud />

### 1. React 生命周期和 Vue 生命周期有什么不同？

**React 生命周期（类组件）：**
- 挂载阶段：constructor → getDerivedStateFromProps → render → componentDidMount
- 更新阶段：getDerivedStateFromProps → shouldComponentUpdate → render → getSnapshotBeforeUpdate → componentDidUpdate
- 卸载阶段：componentWillUnmount

**Vue 生命周期：**
- 创建阶段：beforeCreate → created → beforeMount → mounted
- 更新阶段：beforeUpdate → updated
- 销毁阶段：beforeUnmount → unmounted

**主要不同：**
- Vue 生命周期更细粒度，React 相对简洁
- Vue 在 created 后数据已响应式，React 需要手动处理
- Vue 自动依赖追踪，React 需要 shouldComponentUpdate 优化
- React 函数组件用 useEffect 模拟生命周期，Vue3 用 Composition API

### 2. useState 底层原理
useState 是 React Hooks 中的一个，其底层原理依赖于 React 的渲染周期和闭包。

在函数组件中，每次渲染都会调用函数组件。React 使用一个链表来记录组件的每一个 Hook。每次调用 useState，都会从链表中读取当前 Hook 的状态（如果是第一次渲染，则使用初始值），然后返回状态值和更新函数。

更新函数（setState）会接收新的状态值，并将组件重新加入更新队列，触发重新渲染。在重新渲染时，useState 会返回更新后的状态值。

React 通过顺序来保证多个 useState 调用的正确性，因此不能在条件语句或循环中调用 Hook。
```javascript
let state = [];
let index = 0;

function useState(initialValue) {
  const currentIndex = index;
  
  if (state[currentIndex] === undefined) {
    state[currentIndex] = initialValue;
  }
  
  const setState = (newValue) => {
    state[currentIndex] = newValue;
    render();
  };
  
  index++;
  return [state[currentIndex], setState];
}

function render() {
  index = 0;
  // 组件渲染逻辑
}
```

**核心机制：**
- 基于闭包和顺序调用
- 每次渲染维护状态数组和当前索引
- setState 触发重新渲染时按相同顺序读取状态

### 3. useState 为什么是数组而不是对象

```javascript
// 数组解构
const [count, setCount] = useState(0);
const [name, setName] = useState('');

// 对象解构（不推荐）
const { state: count, setState: setCount } = useState(0);
const { state: name, setState: setName } = useState('');
```

**数组优势：**
- 命名自由：可以自定义变量名
- 简洁性：代码更简洁
- 顺序保证：依赖调用顺序，符合 Hooks 设计理念
- 类型安全：TypeScript 类型推断更准确

### 4. React组件渲染顺序

```javascript
function Parent() {
  console.log('Parent render');
  return <Child />;
}

function Child() {
  console.log('Child render');
  return <div>Child</div>;
}

// 输出顺序：
// Parent render
// Child render
```

**渲染规则：**
- 父组件 → 子组件（深度优先）
- render → DOM 更新 → useEffect（异步执行）
- 兄弟组件按代码顺序渲染

### 5. React数据传输方式

**Props 传递（父子）：**
```javascript
function Parent() {
  return <Child message="Hello" />;
}
function Child({ message }) {
  return <div>{message}</div>;
}
```

**Context API（跨层级）：**
```javascript
const ThemeContext = createContext();
function App() {
  return (
    <ThemeContext.Provider value="dark">
      <Toolbar />
    </ThemeContext.Provider>
  );
}
function Toolbar() {
  const theme = useContext(ThemeContext);
  return <div>{theme}</div>;
}
```

**状态管理库：** Redux、Zustand、MobX 等

**事件总线/发布订阅：**
```javascript
const EventBus = {
  events: {},
  emit(event, data) {
    if (this.events[event]) {
      this.events[event].forEach(cb => cb(data));
    }
  },
  on(event, callback) {
    if (!this.events[event]) this.events[event] = [];
    this.events[event].push(callback);
  }
};
```

### 6. 图片加载优化方式

**懒加载：**
```javascript
const LazyImage = ({ src, alt }) => {
  const [isVisible, setIsVisible] = useState(false);
  const imgRef = useRef();

  useEffect(() => {
    const observer = new IntersectionObserver(([entry]) => {
      if (entry.isIntersecting) {
        setIsVisible(true);
        observer.disconnect();
      }
    });
    observer.observe(imgRef.current);
    return () => observer.disconnect();
  }, []);

  return <img ref={imgRef} src={isVisible ? src : 'placeholder.jpg'} alt={alt} />;
};
```

**图片格式优化：** WebP、AVIF 等现代格式

**响应式图片：**
```html
<img 
  src="image.jpg" 
  srcset="image-320w.jpg 320w, image-640w.jpg 640w"
  sizes="(max-width: 600px) 320px, 640px"
/>
```

### 7. 当大量图片、标签渲染时，每次修改都会造成重渲染，如何优化？

**虚拟列表：**
```javascript
function VirtualList({ items, itemHeight, containerHeight }) {
  const [scrollTop, setScrollTop] = useState(0);
  const visibleStart = Math.floor(scrollTop / itemHeight);
  const visibleEnd = Math.min(visibleStart + Math.ceil(containerHeight / itemHeight) + 1, items.length);
  const visibleItems = items.slice(visibleStart, visibleEnd);
  
  return (
    <div style={{ height: containerHeight, overflow: 'auto' }} onScroll={(e) => setScrollTop(e.target.scrollTop)}>
      <div style={{ height: items.length * itemHeight, position: 'relative' }}>
        {visibleItems.map((item, index) => (
          <div key={item.id} style={{ position: 'absolute', top: (visibleStart + index) * itemHeight, height: itemHeight }}>
            {item.content}
          </div>
        ))}
      </div>
    </div>
  );
}
```

**其他优化：**
- React.memo 避免不必要的重渲染
- useMemo 缓存计算结果
- 防抖加载

### 8. 网络波动导致无法请求数据，怎么缓存（大量数据）导致用户刷新时还能看到数据？

**IndexedDB 缓存：**
```javascript
class DataCache {
  constructor(dbName = 'AppCache', version = 1) {
    this.dbName = dbName;
    this.version = version;
  }

  async init() {
    return new Promise((resolve, reject) => {
      const request = indexedDB.open(this.dbName, this.version);
      request.onerror = () => reject(request.error);
      request.onsuccess = () => {
        this.db = request.result;
        resolve(this.db);
      };
      request.onupgradeneeded = (event) => {
        const db = event.target.result;
        if (!db.objectStoreNames.contains('apiCache')) {
          db.createObjectStore('apiCache', { keyPath: 'key' });
        }
      };
    });
  }

  async set(key, data) {
    const transaction = this.db.transaction(['apiCache'], 'readwrite');
    const store = transaction.objectStore('apiCache');
    return store.put({ key, data, timestamp: Date.now() });
  }

  async get(key, maxAge = 5 * 60 * 1000) {
    return new Promise((resolve) => {
      const transaction = this.db.transaction(['apiCache'], 'readonly');
      const store = transaction.objectStore('apiCache');
      const request = store.get(key);
      request.onsuccess = () => {
        const result = request.result;
        if (result && Date.now() - result.timestamp < maxAge) {
          resolve(result.data);
        } else {
          resolve(null);
        }
      };
    });
  }
}
```

### 9. 当大量请求在循环处理时，如何边渲染边请求，让用户达到无感的体验？

**分批处理 + 进度显示：**
```javascript
function BatchProcessor({ items }) {
  const [processed, setProcessed] = useState([]);
  const [progress, setProgress] = useState(0);
  const [isProcessing, setIsProcessing] = useState(false);

  const processInBatches = async () => {
    setIsProcessing(true);
    const batchSize = 10;
    const totalBatches = Math.ceil(items.length / batchSize);
    
    for (let i = 0; i < totalBatches; i++) {
      const batch = items.slice(i * batchSize, (i + 1) * batchSize);
      const batchResults = await Promise.all(batch.map(item => processItem(item)));
      setProcessed(prev => [...prev, ...batchResults]);
      setProgress(((i + 1) / totalBatches) * 100);
      await new Promise(resolve => setTimeout(resolve, 0));
    }
    setIsProcessing(false);
  };

  return (
    <div>
      <button onClick={processInBatches} disabled={isProcessing}>
        {isProcessing ? `Processing... ${progress.toFixed(1)}%` : 'Start'}
      </button>
      <div>{processed.map(item => <div key={item.id}>{/* 渲染内容 */}</div>)}</div>
    </div>
  );
}
```

### 10. 事件总线

```javascript
class EventBus {
  constructor() {
    this.events = new Map();
  }

  on(event, callback) {
    if (!this.events.has(event)) {
      this.events.set(event, new Set());
    }
    this.events.get(event).add(callback);
    return () => this.off(event, callback);
  }

  off(event, callback) {
    if (this.events.has(event)) {
      this.events.get(event).delete(callback);
    }
  }

  emit(event, data) {
    if (this.events.has(event)) {
      this.events.get(event).forEach(callback => {
        try {
          callback(data);
        } catch (error) {
          console.error(`Error in event listener for ${event}:`, error);
        }
      });
    }
  }

  once(event, callback) {
    const onceWrapper = (data) => {
      callback(data);
      this.off(event, onceWrapper);
    };
    this.on(event, onceWrapper);
  }

  clear() {
    this.events.clear();
  }
}
```

### 11. axios 封装逻辑

```javascript
import axios from 'axios';

const http = axios.create({
  baseURL: process.env.REACT_APP_API_BASE_URL,
  timeout: 10000,
  headers: { 'Content-Type': 'application/json' },
});

http.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('token');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    if (config.method === 'get') {
      config.params = { ...config.params, _t: Date.now() };
    }
    return config;
  },
  (error) => Promise.reject(error)
);

http.interceptors.response.use(
  (response) => response.data,
  (error) => {
    if (error.response) {
      switch (error.response.status) {
        case 401: window.location.href = '/login'; break;
        case 403: console.error('无权限访问'); break;
        case 500: console.error('服务器错误'); break;
        default: console.error('请求错误', error.message);
      }
    } else if (error.request) {
      console.error('网络错误，请检查网络连接');
    } else {
      console.error('请求配置错误', error.message);
    }
    return Promise.reject(error);
  }
);

export const api = {
  get: (url, params) => http.get(url, { params }),
  post: (url, data) => http.post(url, data),
  put: (url, data) => http.put(url, data),
  delete: (url) => http.delete(url),
};
```

### 12. websocket 封装逻辑

```javascript
class WebSocketService {
  constructor(url, options = {}) {
    this.url = url;
    this.options = options;
    this.socket = null;
    this.reconnectAttempts = 0;
    this.maxReconnectAttempts = options.maxReconnectAttempts || 5;
    this.reconnectInterval = options.reconnectInterval || 3000;
    this.messageHandlers = new Set();
    this.isConnected = false;
  }

  connect() {
    return new Promise((resolve, reject) => {
      try {
        this.socket = new WebSocket(this.url);
        this.socket.onopen = (event) => {
          this.isConnected = true;
          this.reconnectAttempts = 0;
          resolve(event);
        };
        this.socket.onmessage = (event) => {
          try {
            const data = JSON.parse(event.data);
            this.messageHandlers.forEach(handler => handler(data));
          } catch (error) {
            console.error('WebSocket message parse error:', error);
          }
        };
        this.socket.onclose = (event) => {
          this.isConnected = false;
          this.handleReconnect();
        };
        this.socket.onerror = (error) => reject(error);
      } catch (error) {
        reject(error);
      }
    });
  }

  handleReconnect() {
    if (this.reconnectAttempts < this.maxReconnectAttempts) {
      this.reconnectAttempts++;
      setTimeout(() => this.connect().catch(error => console.error('Reconnection failed:', error)), this.reconnectInterval);
    }
  }

  send(data) {
    if (this.isConnected && this.socket) {
      this.socket.send(JSON.stringify(data));
    } else {
      console.error('WebSocket is not connected');
    }
  }

  onMessage(handler) {
    this.messageHandlers.add(handler);
    return () => this.messageHandlers.delete(handler);
  }

  close() {
    if (this.socket) {
      this.socket.close();
      this.socket = null;
      this.isConnected = false;
    }
  }
}
```

### 13. webpack loader和plugin区别

**Loader：**
```javascript
module.exports = function(source) {
  return source.replace(/console\.log\(.*\);/g, '');
};

// webpack.config.js
module.exports = {
  module: {
    rules: [
      {
        test: /\.js$/,
        use: ['babel-loader', path.resolve('./my-loader.js')]
      }
    ]
  }
};
```

**Plugin：**
```javascript
class MyPlugin {
  apply(compiler) {
    compiler.hooks.done.tap('MyPlugin', (stats) => {
      console.log('编译完成！');
    });
  }
}

// webpack.config.js
module.exports = {
  plugins: [
    new HtmlWebpackPlugin({ template: './src/index.html' }),
    new MyPlugin(),
    new CleanWebpackPlugin()
  ]
};
```

**区别：**
- Loader：处理单个文件，文件转换器，在 module.rules 中配置
- Plugin：处理整个构建过程，功能更广泛，在 plugins 数组中配置
- Loader 在模块加载时工作，Plugin 在 webpack 的整个编译周期都起作用